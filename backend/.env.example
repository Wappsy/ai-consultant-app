# Backend environment variables example
# Copy this file to .env and fill in the values before running the server

# The port the Express server should listen on
PORT=5001

# MongoDB connection string. For local development you can use:
# MONGODB_URI=mongodb://localhost/ai-consultant

# For MongoDB Atlas use a connection string like the following (replace <user>, <password>, and <cluster>)
# Example:
# MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/ai-consultant?retryWrites=true&w=majority

# If using Atlas, make sure to:
# 1. Create a database user with a username and password
# 2. Add your IP address to the Network Access (or allow 0.0.0.0/0 for testing)
# 3. Replace the placeholder values in the MONGODB_URI above

JWT_SECRET=your-secret-key-here
OPENAI_API_KEY=your-openai-api-key-here
